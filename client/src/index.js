import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

//rendering react component App to the id of root in the index.html file
ReactDOM.render(
  <App />,
  document.getElementById('root')
);